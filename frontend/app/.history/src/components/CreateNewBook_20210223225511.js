import React from 'react'

const CreateNewBook = () => {
    return (
        <div>
            <button onClick={() => createBook}>本を追加する</button>
        </div>
    )
}

export default CreateNewBook
